package com.example.freshegokidproject

import android.content.Context
import androidx.appcompat.widget.AppCompatImageView

class DiscountBannerView(context: Context) : AppCompatImageView(context) {
    // don't display image until fetch asynchronously image when about instantiated succeeded
    //
}